import { CATEGORY_MAP } from 'constants/currency';

export const CATEGORY_FILTERS = [
	CATEGORY_MAP.crypto,
	CATEGORY_MAP.forex,
	CATEGORY_MAP.equities,
	CATEGORY_MAP.commodity,
];

export const SYNTHS_REFRESH_INTERVAL_MS = 30000;
