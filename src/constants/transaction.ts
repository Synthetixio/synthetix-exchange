export const TRANSACTION_STATUS = {
	WAITING: 'waiting',
	PENDING: 'pending',
	CONFIRMED: 'confirmed',
	FAILED: 'failed',
	CANCELLED: 'cancelled',
};

export const GAS_LIMIT_BUFFER = 10000;
export const DEFAULT_GAS_LIMIT = 500000;
